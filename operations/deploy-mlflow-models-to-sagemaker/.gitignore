# Virtual environments
.venv/
venv/

# Jupyter noise
.ipynb_checkpoints/

# Local notebook test runs (papermill outputs contain account-specific ARNs)
_test_output/
runs/

# Prebaked-image Dockerfiles (not part of the published sample)
docker/

# Local environment values fetched from the deployed CFN stack (account-specific)
env.sh

# Downloaded at runtime by 02_deploy_modelbuilder_repack.ipynb (contains the model pickle)
mlflow_model_download/

# Local review notes, not part of the published sample
gpt-review.md

# Generated by 00_setup_and_train.ipynb at runtime — tracked intentionally,
# but ignore common Python junk around it
__pycache__/
*.pyc
